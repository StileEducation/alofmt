[[1, 2], [3, 4]].map do |value|
        school_code = value[0]
        [school_code]
    end
    .to_h
[[1, 2], [3, 4]].map { |value| value }.to_h.foo.bar
{ a: 1 }.map { |value| value }.to_h.foo
[1].map(
    xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx,
    yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy,
) { |value| value }
    .to_h
    .foo
[1].map(
    xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx,
    yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy,
)
    .to_h(1)
    .foo
[1].map { |value| value }
    .to_h(
        xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx,
    )
    .foo
`/var/lib/compose/run_compose config --services`.strip
    .split("\n")
    .map(&:strip)
    .map(&:strip)
    .map(&:strip)
    .map(&:strip)
`ls`.split(
    xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx,
    yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy,
)
    .to_h
    .foo
'/var/lib/compose/run_compose config --services'
    .strip
    .split("\n")
    .map(&:strip)
    .map(&:strip)
    .map(&:strip)
    .map(&:strip)
